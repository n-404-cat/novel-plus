# 11 测试说明

## 1. 当前现状

本项目后续执行要求已经明确为：只要改代码，就必须执行自动化测试或最接近真实运行场景的可执行验证。

## 2. 当前可确认情况

- 父工程 `pom.xml` 中默认跳过测试
- 仓库以构建与部署说明为主
- 当前仍需继续补齐模块级自动化测试策略

## 3. 强制测试分层

- 单元测试：核心业务逻辑
- 接口测试：前台、后台、爬虫关键接口
- 页面验证：模板页面与关键流程
- 数据验证：数据库初始化与升级脚本
- 回归验证：发布前关键业务链路检查

## 4. 强制错误检查项

每次修改后，除自动化测试外，还必须检查以下内容：

- 页面错误
- 浏览器控制台错误
- 后端错误日志
- 构建输出错误
- 命令行执行错误
- 启动过程中的异常和堆栈

## 5. 默认处理规则

- 发现错误后，不是只记录。
- 发现错误后，默认继续定位并直接修复。
- 修复后，必须重新执行对应验证。

## 6. 验证记录要求

每次任务完成后，至少记录以下内容：

- 执行了什么命令
- 检查了哪些页面或流程
- 是否出现控制台错误
- 是否出现日志错误
- 错误是否已修复
- 当前剩余风险

## 7. 后续维护要求

- 后续新增测试脚本或测试流程时，同步更新本文件
- 若引入自动化测试套件，应补充执行命令、覆盖范围、结果判定规则

## 8. 最近一次验证记录

### 8.1 R-028 品牌 Logo 生成

- 验证日期：2026-06-12
- 资源检查：已生成并覆盖 `templates/orange/static/images/logo.png`
- 资源检查：已生成并覆盖 `templates/orange/static/images/logo_white.png`
- 资源检查：已生成并覆盖 `templates/blue/static/images/logo.png`
- 资源检查：已生成并覆盖 `templates/green/static/images/logo.png`
- 尺寸检查：橙色与蓝色 Logo 保持 `450x100`，绿色 Logo 保持 `800x200`
- 视觉检查：已人工查看生成后的 PNG 资源，内容为“404的猫看小说”，并保留透明底与模板主色风格
- 当前结论：新 Logo 资源已可用于现有模板路径，等待业务侧确认品牌视觉效果

### 8.2 R-030 支付配置后台化（第一阶段）

- 验证日期：2026-06-12
- 配置检查：已将 `novel-front/src/main/resources/application-alipay.yml` 中的支付敏感信息替换为环境变量占位符
- 安全检查：全局搜索历史支付 `appId`、私钥、公钥、旧通知地址，未在当前仓库文件中再次命中
- 编译检查：执行 `mvn -pl novel-front -DskipTests compile`，结果通过
- 当前结论：已完成“移除仓库硬编码敏感支付配置”的第一阶段整改，后续继续推进后台配置管理能力

### 8.3 R-029 网站信息即时生效与缓存清理

- 验证日期：2026-06-12
- 执行命令：`mvn -pl novel-front,novel-admin -am -DskipTests compile`
- 命令结果：通过，`novel-front` 与 `novel-admin` 编译成功，无新增编译错误
- 页面检查：执行 `curl -I http://127.0.0.1:8083/` 返回 `HTTP/1.1 200`
- 页面检查：执行 `curl -s http://127.0.0.1:8083/ | head -n 40`，首页返回正常 HTML，不再出现 Whitelabel Error Page
- 缓存检查：执行 `curl -I http://127.0.0.1:8083/css/base.css`，确认模板静态资源返回 `Cache-Control: no-cache, no-store, must-revalidate`
- 接口检查：执行 `curl -s http://127.0.0.1:8083/cache/refresh/novel@2020117/4`，前台网站配置缓存刷新接口返回成功
- 控制台检查：IDE 诊断已检查本次修改文件，未发现新增 Java/HTML 诊断错误；JS 仅保留历史未使用变量提示
- 日志/命令输出检查：本次编译与 HTTP 探测未出现新的异常堆栈或启动报错
- 当前结论：后台网站信息保存提示已调整为即时生效，并新增后台清理缓存入口；前台网站信息改为短期缓存并支持主动失效，模板静态资源不再强缓存
- 剩余风险：本次未在独立启动的 `novel-admin` 页面中做人工点击回归，若需进一步确认后台按钮联动，可补充联调验证

### 8.4 R-030 支付配置后台化（第二阶段）

- 验证日期：2026-06-12
- 数据结构检查：已新增 `doc/sql/20260612.sql`，包含 `payment_config` 表与后台菜单权限脚本；`doc/sql/novel_plus.sql` 同步补齐初始化结构
- 代码检查：已新增后台 `paymentConfig` 管理页、DAO/Service/Controller、MyBatis XML，以及前台支付配置共享实体、Mapper 和运行期读取服务
- 缓存检查：后台保存与手动清理都会删除 `paymentConfigKey:ALIPAY`，前台读取采用 Redis 300 秒短缓存并支持数据库回源
- 编译检查：执行 `mvn -pl novel-common,novel-front,novel-admin -DskipTests compile`，结果通过
- IDE 诊断检查：本次新增和修改的 Java 文件已检查，未发现新增编译或类型错误；`PayController` 仅保留 Spring 精确映射建议提示
- 命令行输出检查：编译阶段未出现新的异常堆栈，只有仓库原有 Lombok 警告和过时 API 提示
- 页面/控制台/日志检查：当前未启动依赖本地私有数据库的前后台服务，尚未完成后台页面手工点击、浏览器控制台和应用运行日志联调验证
- 当前结论：`R-030` 的后台配置、数据库承载和前台动态读取链路已经落地，现阶段达到“代码完成 + 编译通过”，后续需在本地可用数据库环境下补齐页面级验证和支付宝通知联调

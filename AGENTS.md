# novel-plus 仓库协作规范

## 1. 目标

本规范用于约束后续在 `novel-plus` 仓库中的分析、设计、开发、测试与文档更新流程。

## 2. 强制流程

后续所有新增需求、修改需求、缺陷修复、结构调整，默认遵循以下顺序：

1. 先更新文档。
2. 文档中明确需求目标、影响范围、涉及模块、状态变更。
3. 文档确认后，再修改代码。
4. 代码修改完成后，必须执行自动化测试、页面检查、日志检查、控制台检查、命令行检查。
5. 发现错误后默认直接修复，并同步更新文档与验证记录。

禁止直接跳过文档先改代码。

## 3. 需求文档规则

### 3.1 当前已实现需求

- 仓库中已经存在并且代码已落地的功能，在需求文档中统一标记为 `已完成`。

### 3.2 后续新增需求

- 用户提出的新需求，必须先登记到需求文档。
- 需求文档至少要写清楚：
  - 需求名称
  - 提出日期
  - 需求说明
  - 影响模块
  - 状态
  - 备注

### 3.3 状态定义

- `已完成`：代码已实现，当前仓库已具备该能力。
- `待分析`：用户刚提出，尚未完成方案梳理。
- `待开发`：方案已明确，等待实施。
- `开发中`：已进入代码修改阶段。
- `待验证`：代码已改，等待测试或人工确认。
- `已验证`：自动化测试、页面检查、日志检查、控制台检查、命令行检查均已完成。

## 4. 代码注释规则

### 4.1 中文注释要求

- 后续新增代码或改动代码，必须补充必要的中文注释。
- 注释重点说明“为什么这样做”“这段逻辑处理什么问题”“输入输出或边界条件是什么”。
- 不要写无意义注释，例如“给变量赋值”“调用方法”等废话注释。

### 4.2 注释适用范围

- 控制器中的关键入口逻辑
- Service 中的核心业务逻辑
- 定时任务、缓存、分表、内容存储切换、爬虫规则处理
- 复杂 SQL、模板切换、支付与 AI 相关逻辑

## 5. 文档同步规则

- 需求变化先更新 `doc/02-需求规格说明.md`
- 架构变化同步更新 `doc/03-系统架构设计.md`
- 数据结构变化同步更新 `doc/04-数据库设计.md`
- 接口变化同步更新 `doc/05-接口设计.md`
- 模块内部设计变化同步更新对应模块文档
- 部署方式变化同步更新 `doc/09-部署与运维手册.md`
- 测试与验证结果同步更新 `doc/11-测试说明.md`
- 协作流程变化同步更新 `doc/13-研发协作规范.md`

## 6. 自动化测试与错误监控规则

### 6.1 自动化测试

- 只要发生代码修改，就必须执行自动化测试或最接近真实场景的可执行验证命令。
- 不能只做静态阅读后声称完成。
- 如果当前仓库缺少现成测试，也必须补充最小可执行验证步骤，并记录在文档中。

### 6.2 页面错误监控

- 涉及前台、后台、模板、页面流程的改动，必须检查页面是否报错。
- 必须关注页面白屏、接口失败、静态资源错误、模板渲染错误、按钮流程错误。

### 6.3 控制台与命令行错误监控

- 必须检查浏览器控制台错误。
- 必须检查本地命令执行输出中的报错、告警、异常堆栈。
- 不能忽略已经出现的错误信息。

### 6.4 错误日志监控

- 必须检查应用日志中的异常、错误、堆栈、启动失败信息。
- 对已发现的错误，默认动作不是记录后结束，而是继续定位并直接修复。

### 6.5 默认修复策略

- 页面错误：直接修复
- 控制台错误：直接修复
- 命令行报错：直接修复
- 应用日志错误：直接修复

如果错误与当前任务强相关，不允许只汇报不处理。

## 7. 执行原则

- 先分析，后修改。
- 先文档，后代码。
- 先标记状态，后实施变更。
- 代码和文档一起交付，不允许只改其中一侧。
- 修改完成后，必须带验证结果交付。
